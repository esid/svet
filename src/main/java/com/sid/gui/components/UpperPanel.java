package com.sid.gui.components;

import javax.swing.*;
import java.awt.*;

public class UpperPanel extends JPanel {
    public UpperPanel(Frame main) {
        setPreferredSize(new Dimension(main.getWidth(), 10));
    }
}
